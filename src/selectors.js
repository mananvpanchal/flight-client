export const getReturnJourney = (state) => state.returnJourney;

export const getOrigin = (state) => state.flightResult.origin;

export const getDestination = (state) => state.flightResult.dest;

export const getDepartureDate = (state) => state.flightResult.depDate;

export const getReturnDate = (state) => state.flightResult.retDate;

export const getFlightResult = (state) => state.flightResult.arr;

export const getInitResult = (state) => state.initResult;